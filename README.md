A repo to help me keep track of learning the Folium Python library.

# Introduction

# Getting Started

# Installation

`uv` generic workflow

`uv lock`

`uv sync`

* Initialise a new project

```
uv init --python 3.12
```

* Specify `uv` to run with Jupyter
`uv add --dev ipykernel`



Add necessary packages

I suppose the workflow should be uv add as you go, and then do

`uv pip compile`

at the end?